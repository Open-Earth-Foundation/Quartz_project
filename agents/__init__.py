"""Local Quartz application package.

This folder intentionally stays named `agents/` for repository continuity.
At runtime the package is loaded under the alias `quartz_agents` so the
official OpenAI Agents SDK can still own the top-level `agents` import name.
"""

